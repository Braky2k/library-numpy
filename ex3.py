# Multiplique todos os elementos de um vetor por 2. Use broadcasting para evitar loop. Exemplo: Seja vetor = np.array([1, 2, 3, 4]). O resultado esperado: array([2, 4, 6, 8])

import numpy as np

vetor = np.array([1, 2, 3, 4]) * 2
print(vetor)
